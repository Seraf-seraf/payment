name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.26.x"
  APP_NAME: payment-service
  ARTIFACT_NAME: payment-service-linux-amd64
  BUILD_DIR: bin
  REMOTE_DIR: /opt/payment-service
  REMOTE_CONFIG_PATH: /opt/payment-service/configs/config.yaml

jobs:
  test:
    name: Test, fmt and vet
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Run checks
        uses: ./.github/actions/test

  build:
    name: Build binary
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Build
        uses: ./.github/actions/build
        with:
          app-name: ${{ env.APP_NAME }}
          output-dir: ${{ env.BUILD_DIR }}

      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.BUILD_DIR }}/${{ env.APP_NAME }}
          if-no-files-found: error
          retention-days: 7

  deploy:
    name: Deploy to hosting
    runs-on: ubuntu-latest
    needs: build
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download binary
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.BUILD_DIR }}

      - name: Deploy
        uses: ./.github/actions/deploy
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT || '22' }}
          user: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          remote-dir: ${{ env.REMOTE_DIR }}
          binary-path: ${{ env.BUILD_DIR }}/${{ env.APP_NAME }}
          binary-name: ${{ env.APP_NAME }}
          config-content: ${{ secrets.APP_CONFIG }}
          config-path: ${{ env.REMOTE_CONFIG_PATH }}
